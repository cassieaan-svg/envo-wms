node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Local scratch: a copy of .env taken before the tunnel demo.
.env.backup

# Claude Code local config — editor/agent scratch, not part of the project.
.claude/

# Commissioning drill output (pg_dump files from scripts/commission.mjs).
.commission-backups/
