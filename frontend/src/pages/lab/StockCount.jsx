import { StockCountPage } from '../../components/StockCountPage'

// Lab Stock Count — same component as pharmacy; the section comes from the store.
export function StockCount() { return <StockCountPage /> }
