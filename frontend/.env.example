# Backend API base URL. Auth (login / session / password), all data, and the
# realtime SSE stream run against this Express server, which is backed by the
# local Postgres database. Local dev default is http://localhost:5000.
#
# Local dev: copy this file to ".env" in the frontend/ folder.
VITE_API_URL=http://localhost:5000
